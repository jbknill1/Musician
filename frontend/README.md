# freq-ui (frontend)

Minimal Vite + React frontend that connects to `ws://localhost:8000/ws` and displays the latest `max_freq`.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Open the URL printed by Vite (usually http://localhost:5173).
